apiVersion: v1
kind: Service
metadata:
  name: {{ include "kunal-tic-tac-toe.fullname" . }}
  labels:
    {{- include "kunal-tic-tac-toe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "kunal-tic-tac-toe.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
